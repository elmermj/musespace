name: musespace
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  get:
  cupertino_icons:
  google_fonts:
  hive:
  hive_flutter:
  audio_service:
  dartz:
  path_provider:
  permission_handler:
  intl:
  device_info_plus:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
